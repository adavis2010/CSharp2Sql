using Microsoft.Data.SqlClient;
using System;
using System.Collections.Generic;
using System.Text;

namespace CSharp2Sql { }
    //public class MajorsController(Connection connection) {}
     // this.connection = connection; { 




        //public List<Major> GetAll() {
    //var sql = "SELECT * from Class c";
    //var Major = new List<Class>();

    //var cmd = new SqlCommand(sql, connection.sqlconnection);
    //var reader = cmd.ExecuteReader();
    //while (reader.Read()) {

//          I STOPPED RIGHT HERE BEFORE LUNCH
      
       /* var mjr = new Major();
        mjr.Id = Convert.ToInt32(reader["Id"]);
        mjr.Code = reader["Code"].ToString();
        mjr.Description = reader["Subject"].ToString();
        mjr.MinSAT = null;
        if (reader["Id"] != System.DBNull.Value) {
            cls.InstructorId = Convert.ToInt32(reader.Id"]);
        }
        major.Add(mjr);
    }
    reader.Close();
    return cls; 
       /* public SqlConnection Connection { get; set; }

        public void ExecSelect(); }
            var sql = "SELECT * From Major"
            var cmd = new SqlCommand(sql, SqlConnection);
            var reader = cmd.ExecuteReader();
                while (reader.Read()) {
                    major.Id = Convert.ToInt32(reader["Id"]);
                    Major.Code = reader["Code"].ToString();
                    Major.Description = reader["Description"].ToString();
                    Major.MinSAT = Convert.ToInt32(reader["MinSAT"]);


    }

        reader.Close();
      
     */



















